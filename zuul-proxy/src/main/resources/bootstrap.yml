spring:
  application:
    name: zuul-proxy
  cloud:
    config:
      discovery:
        enabled: true
      label: cloudify-2
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/

