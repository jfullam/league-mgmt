spring:
  application:
    name: stat-service
  cloud:
    config:
      discovery:
        enabled: true
      label: cloudify-2
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/